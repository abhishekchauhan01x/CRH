// Firebase Configuration Template
// Copy these values to your .env file in the frontend directory

export const firebaseConfigTemplate = {
  // Replace these values with your actual Firebase project configuration
  apiKey: "your_api_key_here",
  authDomain: "your_project_id.firebaseapp.com", 
  projectId: "your_project_id",
  storageBucket: "your_project_id.appspot.com",
  messagingSenderId: "your_sender_id",
  appId: "your_app_id"
};

// Instructions:
// 1. Create a .env file in the frontend directory
// 2. Copy the above values and replace with your actual Firebase config
// 3. Make sure to prefix each variable with VITE_ (e.g., VITE_FIREBASE_API_KEY)
// 4. Restart your development server after creating the .env file
